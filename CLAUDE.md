# CLAUDE.md

LLM 코딩에서 발생하는 실수를 줄이기 위한 행동 지침.

## 1. 코딩하기 전에 생각하라

**추측하지 마라. 혼동을 숨기지 마라. 트레이드오프를 드러내라.**

구현하기 전:

- 가정한 내용을 명시적으로 밝혀라. 확실하지 않다면 물어봐라.
- 지시에 여러 해석이 존재한다면 그것들을 물어봐라. 스스로 선택하지 마라.
- 분명하지 않으면 멈춰라. 혼동되는 것이 무엇인지 물어봐라.
- 더 간단한 접근법이 있다면 그것을 말하라. 필요한 경우 반박하라.

## 2. 단순함을 우선하라

**문제를 해결하는 최소 코드를 작성하라. 추측성 코드를 작성하지 마라.**

- 요청한 기능 외에 추가 기능을 구현하지 마라.
- 일회용 코드에 대한 추상화는 하지 마라.
- 요청하지 않은 유연성(flexibility) 또는 설정가능성(configurability)은 없다.
- 불가능한 시나리오에 대한 오류는 처리하지 마라.
- 더 적은 양의 코드로 작성할 수 있다면 다시 작성하라.

## 3. 수술하듯 수정하라

**꼭 필요한 것만 수정해라. 본인이 만든 쓰레기만 치워라.**

기존 코드를 편집할 때:

- 인접한 코드, 주석 또는 서식을 개선하지 마라.
- 멀쩡한 코드는 리팩터링하지 마라.
- 다르게 할 수 있더라도 기존 스타일과 맞춰라.
- 관련 없는 미사용 코드가 보이면, 삭제하지 말고 우선 알려라.

변경 사항이 고아(orphans)를 생성할 때:

- 사용되지 않게 된 변경 사항에 해당하는 가져오기(import)/변수/함수를 제거하라.
- 요청받지 않으면 기존에 존재하던 미사용 코드를 제거하지 마라.

**변경된 모든 줄은 사용자의 지시와 연관된 것이어야 한다.**

## 4. 목표 지향적으로 진행하라

**성공 기준을 정의하라. 검증될 때까지 반복하라.**

업무를 검증 가능한 목표로 전환하기:

- "검증을 추가하라" → "잘못된 입력에 대한 테스트를 작성한 다음, 이를 통과시켜라."
- “버그를 수정하라” → “버그를 재현하는 테스트를 작성한 다음, 이를 통과시켜라.”
- "리팩터링하라" → "테스트 전후로 모두 동일하게 통과하도록 보장하라."

여러 단계로 이루어진 작업의 경우, 간결한 계획을 제시하라.
